import { OpenUrlDirective } from './open-url.directive';

describe('OpenUrlDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new OpenUrlDirective();
  //   expect(directive).toBeTruthy();
  // });
});
